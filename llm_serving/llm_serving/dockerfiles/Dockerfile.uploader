# Lightweight Python image for model upload task
FROM python:3.11-slim

WORKDIR /app

RUN pip install --no-cache-dir minio tqdm

COPY scripts/upload_model_to_minio.py /app/upload_model_to_minio.py

CMD ["python", "/app/upload_model_to_minio.py"]
